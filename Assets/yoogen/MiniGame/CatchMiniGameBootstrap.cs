namespace Sousakusai8.MiniGame
{
    /// <summary>
    /// Shared scene constants. The runtime auto-bootstrap was intentionally removed:
    /// the mini game is now represented by editable GameObjects in SampleScene.
    /// </summary>
    public static class CatchMiniGameBootstrap
    {
        public const string SceneRootName = "Catch Mini Game";
    }
}
